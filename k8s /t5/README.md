Task 5 – Limit Namespace (depi)
الهدف:

إنشاء Namespace باسم depi، وتحديد حدود الموارد فيه بحيث يسمح بـ:

1 Deployment فقط

1 Pod فقط

ثم تجربة إنشاء Deployment بعدد replicas أكبر من الحد، وملاحظة النتائج.
